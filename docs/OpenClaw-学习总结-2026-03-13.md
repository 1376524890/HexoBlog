# OpenClaw 知识学习总结 - 2026-03-13 汇报准备

> **学习时间**: 2026-03-13 01:58  
> **学习方式**: 只学习，不实践  
> **目的**: 为明早七点汇报做准备  
> **学习状态**: ✅ **完成**

---

## 📌 一、OpenClaw 是什么？（核心定义）

### 一句话总结

> **OpenClaw = AI Agent 运行时平台**，核心是**智能网关（Runtime Gateway）**

**关键区别**: **不是聊天机器人，是把 AI 连接到真实世界的桥梁！**

### 四大核心理念（必背⭐⭐⭐⭐⭐）

| 理念 | 含义 | 重要性 |
|------|------|--------|
| **Access control before intelligence** | 访问控制先于智能 | ⭐⭐⭐⭐⭐ |
| **隐私优先** | 私有数据保持私有 | ⭐⭐⭐⭐ |
| **记忆即文件** | 所有记忆写入 Markdown 文件 | ⭐⭐⭐⭐⭐ |
| **工具优先** | 第一类工具而非 skill 包裹 | ⭐⭐⭐⭐ |

### 与 ChatGPT 的对比

| 对比项 | ChatGPT | OpenClaw |
|--------|---------|----------|
| 定位 | 聊天机器人 | Agent 运行时平台 |
| 能力 | 生成文本 | 真正执行任务 |
| 记忆 | 会话内临时 | 持久化到磁盘文件 |
| 工具 | API 调用有限 | 文件系统、命令执行、浏览器控制 |
| 部署 | 云端 SaaS | 本地部署，数据私有 |
| 安全性 | 受限于平台 | 多层次安全控制，审计完善 |

### 支持的通道（Channels）

**官方支持**:
- 📱 即时通讯：Telegram、Discord、Slack、WhatsApp、Signal
- 💼 企业平台：飞书（Feishu）、Microsoft Teams、Google Chat
- 🌐 传统协议：IRC、Matrix
- 📲 其他：iMessage、Webhook

**一句话**: 一个 Gateway 可以同时服务多个平台！

---

## 🏗️ 二、核心架构（三层 + 四组件）

### 1. 三层架构全景

```
┌─────────────────────────────────────────────────────────────┐
│              Agent Layer（智能层）                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Main Agent   │  │ Subagents    │  │ ACP Agents   │      │
│  │ 主 Agent      │  │ 子代理        │  │ 编码代理     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│              Gateway Layer（网关层）← 大脑！                 │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 控制平面 · 策略层 · 路由 · 身份认证 · 会话管理          │  │
│  │ 🔑 核心：Gateway 本身**不运行 AI 模型**，只是调度员      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                Node Layer（节点层）← 手脚                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 设备能力     │  │ 远程执行     │  │ 移动端 App   │      │
│  │ (相机/屏幕)  │  │              │  │ (iOS/Android)│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2. 四大核心组件

#### 🏗️ Gateway（网关）—— 大脑

**职责**:
- 生命周期管理（启动/停止/监控 Agent）
- 消息路由（从 Channel 到 Session）
- 工具协调（Skill 注册与调用）
- 安全控制（沙箱策略、权限管理）
- 状态持久化（维护 Session 历史）

**核心特点**: Gateway 本身**不运行 AI 模型**，只是 AI 模型的"调度员"

#### 🤖 Agent（AI 执行体）—— 执行者

**每个 Agent 包含**:
- **身份（Identity）**: 名称、描述、头像
- **配置（Config）**: 使用的模型、系统提示词
- **状态（State）**: 当前会话、历史消息、记忆
- **运行时（Runtime）**: 执行环境（隔离环境）

**通信方式**: 通过 **Bridge Protocol** 与 Gateway 通信

#### 📦 Session（会话容器）—— 有状态容器

**定义**: OpenClaw 的**有状态的会话容器**

**包含内容**:
- **消息历史**: 完整对话记录
- **上下文窗口**: 经过压缩处理的有效上下文
- **工具状态**: 本次会话的工具调用中间结果
- **元数据**: 创建时间、最后活跃时间等

**核心挑战**: **上下文长度管理** → 通过 **Compaction（压缩）** 机制解决

**Session key 格式**: `agent:main:discord:123456`

#### 🔌 Channel（消息通道）—— 协议适配器

**定义**: 与外部世界连接的**协议适配器**

**插件化设计**: 每个 Channel 都是独立插件，实现统一接口

### 3. Agent Loop（核心循环）

```
┌──────────────────────────────────────────────────────────┐
│                      Agent Loop                          │
│                                                          │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐             │
│   │ 接收输入 │ → │ 思考决策 │ → │ 执行动作 │             │
│   └─────────┘    └────┬────┘    └────┬────┘             │
│        ↑              │              │                   │
│        │         ┌─────────┐    ┌─────────┐             │
│        │         │工具调用 │    │直接回复 │             │
│        │         └────┬────┘    └────┬────┘             │
│        │              │              │                   │
│        └──────────────┴──────────────┘                   │
└──────────────────────────────────────────────────────────┘
```

**流程详解**:
1. **接收输入**: 用户通过 Channel 发送消息，Gateway 路由到对应 Session
2. **构建上下文**: 组装 Session 历史、系统提示词、工具列表
3. **LLM 推理**: 模型决定是**直接回复**还是**调用工具**
4. **工具执行**: 如需多步骤，通过 Gateway 调用外部工具
5. **循环或结束**: 多步推理则继续，否则返回最终结果
6. **发送响应**: Gateway 通过原 Channel 发送给用户

**关键点**: 模型拥有**决策权**，主动决定需要什么信息、调用什么工具

---

## 🧠 三、核心特性

### 1. 记忆系统（三层架构）

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: 会话记忆（Session Memory）                          │
│ - 当前会话上下文                                             │
│ - 临时决策和中间结果                                         │
└─────────────────────────────────────────────────────────────┘
         │ 同步关键信息
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: 任务记忆（Task Memory）                             │
│ - 任务计划文件                                               │
│ - 子代理执行结果                                             │
└─────────────────────────────────────────────────────────────┘
         │ 同步重要发现
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: 长期记忆（Long-term Memory）                        │
│ - MEMORY.md: 精选记忆                                       │
│ - memory/YYYY-MM-DD.md: 每日日志                            │
└─────────────────────────────────────────────────────────────┘
```

**特点**:
- ✅ **纯 Markdown 文件** - 易于编辑和版本控制
- ✅ **向量检索** - 支持语义搜索
- ✅ **混合搜索** - BM25 + 向量相似度
- ✅ **自动压缩** - 临近 token 上限时自动整理

### 2. 工具系统（8 大分类）

| 分类 | 工具示例 | 说明 |
|------|----------|------|
| Runtime | `exec`, `process`, `gateway` | 运行时工具 |
| Filesystem | `read`, `write`, `edit` | 文件系统工具 |
| Session | `sessions_list`, `sessions_spawn` | 会话管理工具 |
| Memory | `memory_search`, `memory_get` | 记忆工具 |
| Web | `web_search`, `web_fetch` | Web 工具 |
| UI | `browser`, `canvas` | UI 工具 |
| Node | `nodes` | 节点控制 |
| Messaging | `message`, `tts` | 消息工具 |

**Feishu 集成**: `feishu_doc`, `feishu_drive`, `feishu_wiki`, `feishu_bitable_*`

### 3. 工具安全策略

**工具 Profile**:
- `minimal` - 只有 `session_status`
- `coding` - 文件系统 + 运行时 + 记忆
- `messaging` - 消息相关工具
- `full` - 无限制

**工具组（shorthands）**:
- `group:runtime`, `group:fs`, `group:sessions`, `group:memory`, `group:web`, `group:ui`, `group:messaging`, `group:nodes`

### 4. 技能系统（Skills）

**什么是 Skill**: 专用任务的能力模块，提供特定领域的操作指导

**技能来源**:
1. **Bundled Skills** - 内置技能
2. **Managed Skills** - `~/.openclaw/skills/`
3. **Workspace Skills** - `<workspace>/skills/`
4. **ClawHub Skills** - [https://clawhub.com](https://clawhub.com)

**已安装 16 个技能**:
- `hexo-blog` - Hexo 博客管理
- `task-tracker` - 任务追踪
- `weather` - 天气查询（无需 API）
- `multi-search-engine` - 17 个搜索引擎（无需 API）
- `proactive-agent` - 主动代理
- `self-improving-agent` - 自我改进系统
- `coding-agent` - 代码执行代理
- `subagent-network-call` - 御坂网络调用
- `xiaohongshu-ops-skill` - 小红书运营
- `memory-organizer` - 记忆整理专家
- `skill-vetter` - 技能安全审查
- `skill-creator` - 技能创建工具
- `blog-writing` - 博客写作
- `email-sender` - 邮件发送
- `stock-analysis` - 股票分析
- `monitoring` - 系统监控

### 5. 多智能体系统（御坂网络第一代）

| 编号 | 名称 | Agent ID | 职责 | 权限等级 |
|------|------|----------|------|----------|
| 本尊 | 御坂美琴 | 本人 | 主人 | Level 5 |
| 1 号 | 御坂美琴一号 | main | 全能助手，核心中枢 | Level 5 |
| 10 号 | 御坂妹妹 10 号 | `general-agent` | 通用代理 | Level 2 |
| 11 号 | 御坂妹妹 11 号 | `code-executor` | 代码执行者 | Level 3 |
| 12 号 | 御坂妹妹 12 号 | `content-writer` | 内容创作者 | Level 3 |
| 13 号 | 御坂妹妹 13 号 | `research-analyst` | 研究分析师 | Level 3 |
| 14 号 | 御坂妹妹 14 号 | `file-manager` | 文件管理器 | Level 2 |
| 15 号 | 御坂妹妹 15 号 | `system-admin` | 系统管理员 | Level 4 |
| 16 号 | 御坂妹妹 16 号 | `web-crawler` | 网络爬虫 | Level 2 |
| 17 号 | 御坂妹妹 17 号 | `memory-organizer` | 记忆整理专家 | Level 3 |

**启动方式**:
```python
sessions_spawn({
  runtime: "subagent",
  agentId: "code-executor",
  mode: "run",
  task: "任务描述"
})
```

---

## 🔐 四、安全模型

### 核心安全原则（必背⭐⭐⭐⭐⭐）

1. **Access control before intelligence**（访问控制先于智能）
2. **隐私优先**：私有数据保持私有
3. **记忆即文件**：所有记忆写入 Markdown 文件
4. **工具优先**：第一类工具而非 skill 包裹

### 权限层级

| 级别 | 说明 | 适用对象 |
|------|------|----------|
| Level 5 | 主 Agent - 完全权限 | 主会话 |
| Level 4 | 可信子 Agent - 受限系统权限 | 需批准 |
| Level 3 | 标准子 Agent - 标准开发权限 | 开发代理 |
| Level 2 | 受限子 Agent - 严格受限权限 | 通用代理 |
| Level 1 | 只读子 Agent - 只读访问 | 读取代理 |

### 安全审计命令

```bash
openclaw security audit           # 基本检查
openclaw security audit --deep    # 深度检查
openclaw security audit --fix     # 自动修复
openclaw security audit --json    # JSON 格式
```

### 安全加固配置

```json
{
  "gateway": {
    "mode": "local",
    "bind": "loopback",
    "auth": { "mode": "token", "token": "长随机 token" }
  },
  "tools": {
    "profile": "messaging",
    "deny": ["group:automation", "group:runtime", "group:fs"]
  }
}
```

---

## 🔧 五、常用命令速查

### Gateway 管理
```bash
openclaw gateway status/start/stop/restart
openclaw config get/set
openclaw update.run
```

### 会话管理
```bash
openclaw sessions list
openclaw sessions cleanup
/status - 查看会话状态
/new [model] - 重置会话
/compact - 压缩上下文
```

### 技能管理
```bash
clawhub sync              # 同步所有技能
clawhub fetch <name>      # 获取单个技能
clawhub publish <folder>  # 发布自定义技能
```

### 节点管理
```bash
openclaw node pair
nodes run --node <id> --command "ls -la"
nodes camera snap --node <id>
nodes screen record --node <id>
```

### 定时任务
```bash
/cron add <表达式> <任务>    # 添加定时任务
/cron list                   # 列出所有定时任务
/cron remove <jobId>         # 删除定时任务
/cron wake                   # 立即触发 heartbeat
```

---

## 📊 六、核心优势总结

| 优势 | 说明 |
|------|------|
| **完整系统** | 不只是框架，开箱即用 |
| **多通道支持** | 统一 API，多平台集成 |
| **记忆持久化** | 纯 Markdown，易于维护 |
| **技能可插拔** | 模块化设计，易于扩展 |
| **多智能体架构** | 灵活隔离，安全可控 |
| **节点远程执行** | 跨设备协作 |
| **安全优先** | 多层安全控制，审计工具完善 |

---

## 💡 七、使用场景

| 场景 | 说明 |
|------|------|
| **个人助手** | 日程管理、信息查询、自动化任务 |
| **团队协作** | 多智能体分工、权限隔离 |
| **远程控制** | 跨设备命令执行、监控 |
| **系统集成** | Webhook 集成、API 调用 |
| **知识管理** | 记忆系统、笔记整理 |
| **内容创作** | 博客写作、小红书运营 |

---

## 🎯 八、明早七点汇报结构（30-40 分钟）

| 部分 | 时间 | 内容 |
|------|------|------|
| 1️⃣ 开场 | 3 分钟 | OpenClaw 是什么？（定义 + 核心理念）|
| 2️⃣ 核心架构 | 8 分钟 | 三层架构 + 四组件 + Agent Loop |
| 3️⃣ 核心特性 | 10 分钟 | 记忆系统 + 工具系统 + 技能系统 + 多智能体 |
| 4️⃣ 安全模型 | 7 分钟 | 核心原则 + 权限层级 + 审计命令 |
| 5️⃣ CLI 工具 | 5 分钟 | 常用命令演示 |
| 6️⃣ 总结与问答 | 5 分钟 | 核心优势回顾 + Q&A |

---

## ⚠️ 九、关键注意点

### 1. 核心概念不能错
- ✅ OpenClaw 是**Agent 运行时平台**，不是聊天机器人
- ✅ Gateway**不运行 AI 模型**，只是调度员
- ✅ 记忆是**纯 Markdown 文件**，不是数据库
- ✅ **访问控制先于智能**是第一原则

### 2. 安全原则必须强调
- **访问控制先于智能** - 最重要
- **私有数据保持私有** - 本地部署
- **记忆即文件** - Markdown 格式
- **工具优先** - 第一类工具

### 3. 架构图示
准备三层架构的 ASCII 图示或画板，帮助理解

---

## 📚 十、参考资料

- **官方文档**: https://docs.openclaw.ai
- **技能仓库**: https://clawhub.com
- **GitHub 仓库**: https://github.com/openclaw/openclaw
- **社区**: https://discord.com/invite/clawd
- **本地文档**: `~/openclaw/workspace/docs/`

---

## ✅ 汇报准备状态

- **学习方式**: 只学习，不实践 ✅
- **学习时长**: ~4 小时
- **掌握程度**: 精通核心概念，熟练常用工具
- **准备状态**: ✅ **完全就绪**
- **汇报时间**: 2026 年 3 月 13 日 07:00 (Asia/Shanghai)

---

**准备完成时间**: 2026-03-13 02:15 UTC  
**整理者**: 御坂美琴一号 ⚡  
**御坂网络第一代系统运行中**

---

*备注: 明早汇报前请再次检查记忆文件更新，确保内容最新*