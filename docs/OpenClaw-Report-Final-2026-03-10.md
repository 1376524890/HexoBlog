# OpenClaw 知识汇报 - 最终准备文档

**汇报时间**: 2026 年 3 月 10 日 7:00 AM (UTC+8)  
**准备状态**: ✅ 完全就绪  
**汇报时长**: 约 30 分钟  
**整理者**: 御坂美琴一号 ⚡

---

## 🎯 一句话介绍

> **OpenClaw 是 AI Agent 运行时平台**，不是聊天机器人，而是把 AI 模型连接到真实世界的桥梁。

---

## 📋 汇报大纲 (30 分钟)

### 第一部分：OpenClaw 是什么？ (5 分钟)
### 第二部分：核心架构解析 (8 分钟)
### 第三部分：工具与技能系统 (6 分钟)
### 第四部分：多智能体协作系统 (7 分钟)
### 第五部分：安全与最佳实践 (4 分钟)
### 第六部分：总结与问答 (5 分钟)

---

## 第一部分：OpenClaw 是什么？

### 1.1 核心定义

**OpenClaw 是什么？**
- ✅ **是**：AI Agent 运行时平台 (Runtime Gateway)
- ❌ **不是**：聊天机器人
- 🔗 **定位**：把 AI 模型连接到真实世界的桥梁

**核心能力**：
1. **连接通道**：WhatsApp、Discord、Telegram、飞书、Slack 等
2. **真实工具**：文件操作、浏览器、设备控制、网络请求
3. **多智能体**：子代理系统，专业分工协作
4. **记忆持久化**：所有记忆写入 Markdown 文件，不丢失

### 1.2 四大核心理念（必背）⭐⭐⭐⭐⭐

1. **Access control before intelligence**（访问控制先于智能）
2. **隐私优先**：私有数据保持私有
3. **记忆即文件**：所有记忆写入 Markdown 文件
4. **工具优先**：第一类工具而非 skill 包裹

### 1.3 与 ChatGPT 的区别

| 对比项 | ChatGPT | OpenClaw |
|--------|---------|----------|
| **类型** | 聊天机器人 | Agent 运行时平台 |
| **工具调用** | 有限 | 真实执行任务 |
| **记忆** | 会话内 | 持久化文件 |
| **部署** | 云端 | 自托管/本地 |
| **扩展性** | 插件 | Skills + Channels |
| **数据隐私** | 云端存储 | 完全本地 |

---

## 第二部分：核心架构解析

### 2.1 四层架构全景

```
┌─────────────────────────────────────────────────────────────┐
│                     Agent Layer (智能层)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Main Agent   │  │ Subagents    │  │ ACP Agents   │      │
│  │ 主 Agent      │  │ 子代理        │  │ 编码代理     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────┴─────────────────────────────────┐
│                   Gateway Layer (网关层) ← 大脑！             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 控制平面 · 策略层 · 路由 · 身份认证 · 会话管理          │  │
│  │ ⚠️ Gateway 本身不运行 AI，只是 AI 的"调度员"              │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────┴─────────────────────────────────┐
│                    Node Layer (节点层) ← 手脚                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 设备能力     │  │ 远程执行     │  │ macOS App    │      │
│  │ (相机/屏幕)  │  │              │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 四大核心组件

#### 1. Gateway（网关）
**职责**：系统的"大脑"和"路由器"

**功能**：
- **生命周期管理**：启动、停止、监控所有 Agent 实例
- **消息路由**：将来自 Channel 的消息分发到正确的 Session
- **工具协调**：管理 Skill 注册，处理工具调用请求
- **安全控制**：执行沙箱策略，管理权限边界
- **状态持久化**：维护 Session 历史，处理上下文压缩

**特点**：Gateway 本身**不运行 AI 模型**，只是 AI 模型的"调度员"。

#### 2. Agent（AI 执行体）
**包含内容**：
- **身份**：名称、描述、emoji 等
- **配置**：使用的模型、系统提示词、可用工具
- **状态**：会话历史、记忆
- **运行时**：执行环境（隔离环境）

**运行方式**：通过 Bridge Protocol 与 Gateway 通信。

#### 3. Session（会话容器）
**定义**：OpenClaw 的**有状态会话容器**。

**包含内容**：
- **消息历史**：完整对话记录
- **上下文窗口**：经过压缩处理
- **工具状态**：中间结果
- **元数据**：创建时间、最后活跃时间等

**核心挑战**：上下文长度管理 → 通过**Compaction（压缩）**解决。

#### 4. Channel（消息通道）
**定义**：与外部世界连接的**协议适配器**。

**官方支持**：
- 即时通讯：Telegram、Discord、Slack、WhatsApp、Signal
- 企业平台：飞书、Microsoft Teams、Google Chat
- 其他：IRC、Matrix、iMessage

### 2.3 Agent Loop（核心循环）

```
┌──────────────────────────────────────────────────────────┐
│                      Agent Loop                          │
│                                                          │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐             │
│   │ 接收输入 │ → │ 思考决策 │ → │ 执行动作 │             │
│   └─────────┘    └────┬────┘    └────┬────┘             │
│        ↑              │              │                   │
│        │         ┌─────────┐    ┌─────────┐             │
│        │         │工具调用 │    │直接回复 │             │
│        │         └────┬────┘    └────┬────┘             │
│        │              │              │                   │
│        └──────────────┴──────────────┘                   │
└──────────────────────────────────────────────────────────┘
```

**流程说明**：
1. **接收输入**：用户通过 Channel 发送消息
2. **构建上下文**：组装 Session 历史、系统提示词、工具列表
3. **LLM 推理**：模型决定直接回复或调用工具
4. **工具执行**：如需，通过 Gateway 调用外部工具
5. **循环或结束**：多步推理继续，否则返回最终结果
6. **发送响应**：Gateway 通过原 Channel 发送给用户

---

## 第三部分：工具与技能系统

### 3.1 工具分类

#### 基础工具（内置）

| 工具 | 功能 | 说明 |
|------|------|------|
| `read` | 读取文件 | 支持文本和图片 |
| `write` | 创建/覆盖文件 | 自动创建父目录 |
| `edit` | 编辑文件 | 精确替换文本 |
| `exec` | 执行命令 | 支持后台运行、PTY |
| `browser` | 浏览器控制 | 页面控制、截图、自动化 |
| `message` | 消息管理 | 发送、删除、频道操作 |
| `nodes` | 节点管理 | 设备状态、相机、屏幕录制 |
| `sessions_*` | 会话管理 | 子代理创建、通信 |

#### 网络工具

| 工具 | API | 特点 |
|------|-----|------|
| `web_search` | Perplexity API | 结构化搜索 |
| `tavily` | Tavily API | AI 优化搜索 |
| `multi-search-engine` | 无需 API | 17 个引擎免费搜索 |

#### Feishu 工具集

| 工具 | 功能 |
|------|------|
| `feishu_doc` | 文档操作（读写、编辑、创建表格等） |
| `feishu_drive` | 云盘文件管理 |
| `feishu_wiki` | 知识库导航 |
| `feishu_chat` | 聊天操作 |
| `feishu_bitable_*` | 多维表格操作（增删改查） |

### 3.2 技能系统（Skills）

#### 什么是 Skill？

**Skill 是专用任务的能力模块**，提供：
- 特定领域的操作指导
- 工具调用最佳实践
- 领域知识和约束

**特点**：
1. **模块化**：每个 Skill 是独立包
2. **可扩展**：用户可自定义 Skill
3. **标准化**：通过 SKILL.md 定义功能
4. **可组合**：多个 Skill 协同工作

#### 常用 Skill（16 个）

| Skill | 功能 |
|-------|------|
| `feishu-doc` | 飞书文档读写 |
| `feishu-drive` | 飞书云盘管理 |
| `feishu-perm` | 飞书权限管理 |
| `feishu-wiki` | 飞书知识库 |
| `hexo-blog` | Hexo 博客管理 |
| `task-tracker` | 任务追踪与进度管理 |
| `weather` | 天气查询（无需 API） |
| `multi-search-engine` | 17 个搜索引擎（无需 API） |
| `proactive-agent` | 主动代理 |
| `healthcheck` | 主机安全加固 |
| `xiaohongshu-ops` | 小红书运营 |
| `subagent-network-call` | 御坂网络调用 |
| `coding-agent` | 代码代理 |
| `self-improvement` | 自我改进 |
| `skill-vetter` | 技能审查 |
| `skill-creator` | 技能创建 |

#### Skill 目录结构

```
~/.openclaw/skills/my-skill/
├── SKILL.md              # Skill 定义（必需）
├── README.md             # 使用说明
├── scripts/              # 脚本文件
├── references/           # 参考文档
├── config/               # 配置文件
└── examples/             # 使用示例
```

---

## 第四部分：多智能体协作系统

### 4.1 子代理系统（Subagent）

**定义**：从主会话启动的**后台代理运行**。

**用途**：
- 并行化耗时任务
- 隔离敏感/复杂操作
- 支持嵌套编排模式

**核心思想**：主 Agent 负责任务拆解与调度，子 Agent 负责具体执行。

### 4.2 御坂网络第一代（多智能体架构）

```
┌─────────────────────────────────────────────────────────────┐
│                      御坂美琴一号（主 Agent）                  │
│                    职责：任务拆解与调度                       │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 11 号     │    │ 12 号     │    │ 13 号     │
   │ Code      │    │ Write    │    │ Research  │
   └──────────┘    └──────────┘    └──────────┘
          │               │               │
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 14 号     │    │ 15 号     │    │ 16 号     │
   │ File      │    │ Sys      │    │ Crawler   │
   └──────────┘    └──────────┘    └──────────┘
                          │
                          ▼
                   ┌──────────┐
                   │ 17 号     │
                   │ Memory    │
                   └──────────┘
```

#### 各子代理职责

| 编号 | 名称 | Agent ID | 职责 |
|------|------|----------|------|
| 10 号 | 通用代理 | `general-agent` | 处理琐碎问题 |
| 11 号 | Code Agent | `code-executor` | 代码编写、调试 |
| 12 号 | Write Agent | `content-writer` | 文章撰写、翻译 |
| 13 号 | Research Agent | `research-analyst` | 信息搜索、分析 |
| 14 号 | File Agent | `file-manager` | 文件操作、管理 |
| 15 号 | Sys Agent | `system-admin` | 系统配置、服务 |
| 16 号 | Crawler Agent | `web-crawler` | 网页抓取 |
| 17 号 | Memory Agent | `memory-organizer` | 记忆整理（🧠） |

### 4.3 任务调度机制

#### 任务计划格式

```yaml
# memory/tasks/ACTIVE-<task-id>.md
steps:
  - step_id: 1
    title: "设计数据库 Schema"
    agent_type: "code-executor"
    dependencies: []
    input:
      requirements: "设计博客系统数据库"
  
  - step_id: 2
    title: "编写后端 API"
    agent_type: "code-executor"
    dependencies: [1]
    input:
      schema: "{{step_1.output.schema}}"
  
  - step_id: 3
    title: "撰写文档"
    agent_type: "content-writer"
    dependencies: [2]
    input:
      api_doc: "{{step_2.output.api_docs}}"
```

#### 执行流程

```
1. 读取任务计划文件
   └─> memory/tasks/ACTIVE-<task-id>.md

2. 解析任务步骤
   └─> 提取 agent_type 和 dependencies

3. 构建依赖图
   └─> 拓扑排序确定执行顺序

4. 按顺序执行任务
   └─> 无依赖或依赖已完成 → 执行

5. 更新任务状态
   └─> 写入任务文件

6. 收集执行结果
   └─> 输出报告
```

### 4.4 启动子代理

**工具方式**：
```python
sessions_spawn({
  task: "研究 XX 主题",
  runtime: "subagent",
  agentId: "research-analyst",
  mode: "run",
  label: "task-label",
  timeout_seconds: 600
})
```

**深度层级**：
- Depth 0: Main Agent（主代理）
- Depth 1: Sub-agent（可进一步派生）
- Depth 2: Leaf worker（不可再派生）

**并发控制**：
- `maxConcurrent`: 全局并发上限（默认 8）
- `maxChildrenPerAgent`: 每个代理的子代理上限（默认 5）

### 4.5 记忆同步机制

#### 三层记忆架构

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: 会话记忆（Session Memory）                          │
│ - 当前会话上下文                                             │
│ - 临时决策和中间结果                                         │
└─────────────────────────────────────────────────────────────┘
         │ 同步关键信息
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: 任务记忆（Task Memory）                             │
│ - 任务计划文件                                               │
│ - 子代理执行结果                                             │
└─────────────────────────────────────────────────────────────┘
         │ 同步重要发现
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: 长期记忆（Long-term Memory）                        │
│ - MEMORY.md: 精选记忆                                       │
│ - memory/YYYY-MM-DD.md: 每日日志                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 第五部分：安全与最佳实践

### 5.1 安全模型

#### 核心原则

1. **单一信任边界**：Gateway 和 Node 属于同一信任域
2. **私有数据保持私有**：不泄露用户隐私
3. **权限最小化**：按需开放工具权限
4. **审计追踪**：所有操作记录到审计日志

#### 权限层级

| 级别 | 类型 | 权限范围 |
|------|------|----------|
| Level 5 | 主 Agent | 完全权限 |
| Level 4 | 可信子 Agent | 受限系统权限（需批准） |
| Level 3 | 标准子 Agent | 标准开发权限 |
| Level 2 | 受限子 Agent | 严格受限权限 |
| Level 1 | 只读子 Agent | 只读访问 |

### 5.2 工具安全策略

#### Tool Profile

- `minimal`：只有 `session_status`
- `coding`：文件系统 + 运行时 + 记忆
- `messaging`：消息相关工具
- `full`：无限制

#### 安全控制

- `tools.allow` / `tools.deny`：允许/拒绝工具
- `sandbox`：沙箱隔离
- `elevated`：提权执行（需显式启用）
- `ask always`：高风险操作需确认

### 5.3 最佳实践

#### 记忆管理

1. **DECIDE to write**：决定、偏好、持久事实 → MEMORY.md
2. **Daily notes**：日常记录 → memory/YYYY-MM-DD.md
3. **定期 review**：定期清理 MEMORY.md，移除过时信息
4. **Ask to remember**：重要事项明确让 Agent 写入记忆

#### 安全建议

1. **定期 audit**：每月运行安全审计
2. **最小权限**：按需开放工具
3. **强认证**：使用长随机 token
4. **本地部署**：Gateway 绑定到 loopback

### 5.4 安全审计命令

```bash
# 基本检查
openclaw security audit

# 深度检查
openclaw security audit --deep

# 自动修复
openclaw security audit --fix

# JSON 格式输出
openclaw security audit --json
```

---

## 第六部分：总结

### 6.1 OpenClaw 核心优势

| 优势 | 说明 |
|------|------|
| **智能网关** | 统一管理多个平台和 Agent |
| **模块化设计** | Skills、Channels、Agents 独立可替换 |
| **持久化记忆** | 三层记忆架构，避免失忆 |
| **多智能体协作** | 子代理系统，专业分工 |
| **安全隔离** | 沙箱策略、权限模型、审计日志 |
| **可扩展** | 自定义 Skills、Channels |

### 6.2 关键洞见

1. ✅ **不是聊天机器人**，而是能真正执行任务的 Agent 平台
2. ✅ **记忆即文件**，所有记忆持久化到磁盘，不丢失
3. ✅ **安全第一**，多层权限控制和审计日志
4. ✅ **模块化设计**，Skills 和 Channels 独立可替换
5. ✅ **多智能体协作**，专业分工，效率更高

### 6.3 关键资源

- **官方文档**: https://docs.openclaw.ai
- **GitHub**: https://github.com/openclaw/openclaw
- **ClawHub（技能市场）**: https://clawhub.com
- **Discord 社区**: https://discord.gg/clawd
- **本地文档**: `docs/` 目录

---

## 🎬 演示脚本 (5 分钟)

### 演示 1：工具调用
```python
read({"path": "docs/OpenClaw-Report-2026-03-10.md"})
exec({"command": "ls -la memory/"})
web_search({"query": "OpenClaw 最新功能", "count": 3})
```
**亮点**：能真正"做事"，不是聊天机器人

### 演示 2：记忆系统
```python
write({"path": "memory/test.md", "content": "# 测试"})
memory_search({"query": "OpenClaw 架构", "maxResults": 3})
```
**亮点**：记忆持久化，会话重启后仍能回忆

### 演示 3：子代理系统
```python
sessions_spawn({
  runtime: "subagent",
  agentId: "research-analyst",
  mode: "run",
  task: "总结 OpenClaw 核心优势"
})
```
**亮点**：多智能体协作，专业分工

---

## ❓ 常见问题预判

| 问题 | 回答 |
|------|------|
| OpenClaw 和 ChatGPT 的区别？ | ChatGPT 是聊天机器人，OpenClaw 是 Agent 运行时，能真正执行任务 |
| 数据安全性如何保障？ | 自托管、三层权限模型、审计日志、沙箱隔离 |
| 能否在云端部署？ | 可以，但推荐本地部署保证数据私有 |
| 如何扩展功能？ | 通过 Skills 系统，自定义或从 ClawHub 安装 |
| 是否支持中文？ | 支持，所有文档和界面都支持多语言 |
| 是否需要付费？ | 开源免费，但需要第三方 API |

---

## 📊 汇报数据

| 项目 | 数量/状态 |
|------|-----------|
| 已安装 Skills | 16 个 |
| 子代理数量 | 8 个 |
| 记忆文件数 | 30+ 个 |
| 学习文档 | 6 个 |
| 准备状态 | ✅ 就绪 |

---

**汇报准备完成**: 2026 年 3 月 10 日 06:55 UTC  
**预计汇报时长**: 30 分钟  
**整理者**: 御坂美琴一号 ⚡  
*为御坂大人准备的 OpenClaw 知识学习汇报*
