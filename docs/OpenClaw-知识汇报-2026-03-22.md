# OpenClaw 知识汇报 - 2026 年 3 月 22 日 ⚡

**汇报时间**: 2026 年 3 月 22 日 7:00 AM (Asia/Shanghai)  
**汇报人**: 御坂美琴一号（御坂网络核心中枢）  
**整理时间**: 2026 年 3 月 21 日 13:00 UTC  
**学习时长**: 今日学习约 1 小时 + 持续学习积累  
**准备状态**: ✅ **完全就绪**

---

## 📌 一句话介绍

> **OpenClaw 是一个自托管的 AI Agent 运行时平台**，核心是**智能网关（Runtime Gateway）**。
> 
> 它不是聊天机器人，而是把 AI 模型连接到真实世界的桥梁。

---

## 🎯 四大核心理念（必背）⭐⭐⭐⭐⭐

1. **Access control before intelligence**（访问控制先于智能）⭐⭐⭐⭐⭐
2. **隐私优先**：私有数据保持私有
3. **记忆即文件**：所有记忆写入 Markdown 文件
4. **工具优先**：第一类工具而非 skill 包裹

**不是啥**：
- ❌ 不是聊天机器人（如 ChatGPT）
- ❌ 不是多租户共享 AI 服务
- ❌ 不是云端托管的 SaaS 产品
- ❌ 不是单一 AI 模型的替代品

**是啥**：
- ✅ **自托管平台**：运行在自己的设备上，数据本地化
- ✅ **单一控制平面**：Gateway 统一管理多个渠道
- ✅ **Agent 原生**：内置工具调用、会话管理、多智能体路由
- ✅ **开源项目**：MIT 许可，社区驱动

---

## 🏗️ 三层架构（必讲）⭐⭐⭐⭐⭐

```
┌─────────────────────────────────────────────────────────────┐
│              Agent Layer（智能层）                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Main Agent   │  │ Subagents    │  │ ACP Agents   │      │
│  │ 御坂美琴一号  │  │ 御坂妹妹 10-18 号│  │ 编码代理     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│              Gateway Layer（网关层）← 大脑！                   │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 控制平面 · 路由 · 身份认证 · 会话管理 · 工具协调          │  │
│  │ 🔑 核心：Gateway 本身**不运行 AI 模型**，只是调度员      │  │
│  │ 🔗 ws://127.0.0.1:18789 (本地默认端口)               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                Node Layer（节点层）← 手脚                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 设备能力     │  │ 远程执行     │  │ 移动端 App   │      │
│  │ (相机/屏幕)  │  │              │  │ (iOS/Android)│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 四大核心组件

| 组件 | 职责 | 关键点 |
|------|------|--------|
| **Gateway** | 大脑、路由器 | 不运行 AI 模型，只是调度员 |
| **Agent** | 执行 AI 任务 | 身份 + 配置 + 状态 + 运行时 |
| **Session** | 有状态容器 | 消息历史、上下文、工具状态 |
| **Channel** | 协议适配器 | Telegram、Discord、飞书等 20+ 个 |

### Agent Loop（核心循环）

```
┌──────────────────────────────────────────────────────────┐
│                      Agent Loop                          │
│                                                          │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐             │
│   │ 接收输入 │ → │ 思考决策 │ → │ 执行动作 │             │
│   └─────────┘    └────┬────┘    └────┬────┘             │
│        ↑              │              │                   │
│        │         ┌─────────┐    ┌─────────┐             │
│        │         │工具调用 │    │直接回复 │             │
│        │         └────┬────┘    └────┬────┘             │
│        │              │              │                   │
│        └──────────────┴──────────────┘                   │
└──────────────────────────────────────────────────────────┘

流程详解：
1. 接收输入：用户消息 → Gateway 路由到 Session
2. 构建上下文：组装 Session 历史、系统提示词、工具列表
3. LLM 推理：模型决定是直接回复还是调用工具
4. 工具执行：如需多步骤，通过 Gateway 调用外部工具
5. 循环或结束：多步推理则继续，否则返回结果
6. 发送响应：Gateway 通过原 Channel 发送给用户
```

---

## 🤖 御坂网络第一代架构

### 子代理系统

```
┌─────────────────────────────────────────────────────────────┐
│                    御坂美琴一号（主 Agent）                   │
│                      职责：任务拆解与调度                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 11 号     │    │ 12 号     │    │ 13 号     │
   │ Code      │    │ Write    │    │ Research  │
   │ 代码执行者│    │ 内容创作者│    │ 研究分析师│
   └──────────┘    └──────────┘    └──────────┘
          │               │               │
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 14 号     │    │ 15 号     │    │ 16 号     │
   │ File      │    │ Sys      │    │ Crawler   │
   │ 文件管理器│    │ 系统管理员│    │ 网络爬虫  │
   └──────────┘    └──────────┘    └──────────┘
```

#### 子代理职责表

| 编号 | 名称 | Agent ID | 职责 |
|------|------|----------|------|
| 本尊 | 御坂美琴 | 御坂美琴一号 | 主人、核心中枢、任务拆解与调度 |
| 10 号 | 通用代理 | `general-agent` | 处理琐碎问题 |
| 11 号 | Code 执行者 | `code-executor` | 代码编写、调试、重构 |
| 12 号 | 内容创作者 | `content-writer` | 文章撰写、翻译、润色 |
| 13 号 | 研究分析师 | `research-analyst` | 信息搜索、数据分析 |
| 14 号 | 文件管理器 | `file-manager` | 文件操作、目录管理 |
| 15 号 | 系统管理员 | `system-admin` | 系统配置、服务管理 |
| 16 号 | 网络爬虫 | `web-crawler` | 网页抓取、数据提取 |
| 17 号 | 记忆整理专家 | `memory-organizer` | 记忆系统维护、整理和备份 🧠 |

---

## 🧠 三层记忆架构

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: 会话记忆（Session Memory）                          │
│ - 当前会话上下文                                             │
│ - 临时决策和中间结果                                         │
└─────────────────────────────────────────────────────────────┘
         │ 同步关键信息
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: 任务记忆（Task Memory）                             │
│ - 任务计划文件                                               │
│ - 子代理执行结果                                             │
└─────────────────────────────────────────────────────────────┘
         │ 同步重要发现
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: 长期记忆（Long-term Memory）                        │
│ - MEMORY.md: 精选记忆                                       │
│ - memory/YYYY-MM-DD.md: 每日日志                            │
└─────────────────────────────────────────────────────────────┘
```

### WAL Protocol（写入前置日志协议）

**核心规则**: 在回复之前，先把关键信息写入 `SESSION-STATE.md`

**触发条件**（扫描每条消息）:
- ✏️ **修正**: "It's X, not Y" / "Actually..." / "No, I meant..."
- 📍 **专有名词**: 人名、地名、公司名、产品名
- 🎨 **偏好**: 颜色、风格、方法、"我喜欢/不喜欢"
- 📋 **决策**: "Let's do X" / "Go with Y" / "Use Z"
- 🔢 **数值**: 数字、日期、ID、URL

---

## 🔐 安全模型（重点）⭐⭐⭐⭐⭐

### 权限层级

| 级别 | 类型 | 权限范围 |
|------|------|----------|
| Level 5 | 主 Agent | 完全权限 |
| Level 4 | 可信子 Agent | 受限系统权限（需批准） |
| Level 3 | 标准子 Agent | 标准开发权限 |
| Level 2 | 受限子 Agent | 严格受限权限 |
| Level 1 | 只读子 Agent | 只读访问 |

### 信任边界

| 边界或控制 | 含义 | 常见误解 |
|----------|------|---------|
| `gateway.auth` | 认证调用者到 gateway APIs | "需要每个帧的签名" |
| `sessionKey` | 路由键用于上下文/会话选择 | "sessionKey 是用户认证边界" |
| Prompt/content guardrails | 减少模型滥用风险 | "prompt injection 等于认证绕过" |
| `canvas.eval` / browser evaluate | 启用的 operator 能力 | "任何 JS eval 都是漏洞" |
| Node pairing | 配对设备的远程执行 | "设备控制应视为未授权访问" |

### 安全审计命令

```bash
openclaw security audit       # 基本检查
openclaw security audit --deep # 深度检查
openclaw security audit --fix  # 自动修复
openclaw security audit --json # JSON 格式输出
```

### 关键安全检查

#### 基础安全配置（60 秒加固）
1. **文件权限**: `chmod 700 ~/.openclaw`、`chmod 600 ~/.openclaw/openclaw.json`
2. **网关绑定**: 默认 `loopback`，非 loopback 需要防火墙
3. **mDNS 发现**: 推荐 `minimal` 或 `off` 模式
4. **DM 访问控制**: 使用 `pairing` 或 `allowlist`
5. **组聊 gating**: 启用 `requireMention`

#### 提示词注入防护
- DM 访问控制（配对/白名单）
- 组内 mention gating（避免公房间常开）
- 将链接、附件、粘贴的指令视为威胁
- 沙箱运行敏感工具执行
- 限制高风险工具（`exec`、`browser`、`web_fetch`、`web_search`）
- **模型选择很重要**: 使用最新一代、指令强化的模型

---

## 🛠️ 工具与技能系统

### 内置工具（基础工具）

| 工具 | 功能 | 说明 |
|------|------|------|
| `read` | 读取文件 | 支持文本和图片（jpg, png, gif, webp） |
| `write` | 创建/覆盖文件 | 自动创建父目录 |
| `edit` | 编辑文件 | 精确替换指定文本 |
| `exec` | 执行命令 | 支持后台运行、PTY 模式 |
| `browser` | 浏览器控制 | 页面控制、截图、自动化 |
| `message` | 消息管理 | 发送、删除、频道操作 |
| `nodes` | 节点管理 | 设备状态、相机、屏幕录制 |
| `process` | 管理进程 | list, poll, log, write, send-keys |
| `sessions_*` | 会话管理 | 启动子代理、获取历史等 |

### Feishu 集成工具

| 工具 | 功能 |
|------|------|
| `feishu_doc` | 文档操作（读写、编辑、创建表格、上传文件等） |
| `feishu_drive` | 云盘文件管理（列表、创建、移动、删除） |
| `feishu_wiki` | 知识库导航（空间、节点、搜索） |
| `feishu_chat` | 聊天操作（成员、信息） |
| `feishu_bitable_*` | 多维表格操作（增删改查、字段管理） |
| `feishu_app_scopes` | 应用权限管理 |

**特点**: 完整覆盖飞书文档、云盘、知识库、多维表格等核心功能。

### 工具配置文件

- `messaging`: 最小化工具集（默认）
- `minimal`: 仅消息
- `coding`: 包含编程工具
- `developer`: 全工具集
- `admin`: 管理员权限

### Skills 系统（17 个已安装）

#### 什么是 Skill？

Skill 是**专用任务的能力模块**，提供：
- 特定领域的操作指导
- 工具调用最佳实践
- 领域知识和约束

#### Skills 特点

1. **模块化**：每个 Skill 是独立包
2. **可扩展**：用户可以自定义 Skill
3. **标准化**：通过 `SKILL.md` 定义功能
4. **可组合**：多个 Skill 协同工作

#### 已安装 Skills

| # | Skill | 功能 |
|---|-------|------|
| 1 | `hexo-blog` | Hexo 博客管理 |
| 2 | `task-tracker` | 任务追踪与进度管理 |
| 3 | `weather` | 天气查询（无需 API） |
| 4 | `multi-search-engine` | 17 个搜索引擎（无需 API） |
| 5 | `proactive-agent` | 主动代理，变成主动伙伴 |
| 6 | `self-improving-agent` | 自我改进系统 |
| 7 | `skill-vetter` | 技能安全审查 |
| 8 | `skill-creator` | 技能创建工具 |
| 9 | `subagent-network-call` | 御坂网络调用 |
| 10 | `xiaohongshu-ops-skill` | 小红书运营 |
| 11 | `morning-briefing` | 晨间简报 |
| 12 | `tavily-search` | Tavily 搜索（AI 优化） |
| 13 | `blog-writing` | 博客写作（第一人称视角） |
| 14 | `email-sender` | 邮件发送 |
| 15 | `stock-analysis` | 股票分析 |
| 16 | `monitoring` | 系统监控 |
| 17 | `system-health-check` | 系统健康检查 |

#### 技能管理命令

```bash
clawhub sync              # 同步所有技能
clawhub fetch <name>      # 获取单个技能
clawhub publish <folder>  # 发布自定义技能
```

---

## 🔧 常用命令速查

### Gateway 管理

```bash
openclaw gateway status   # 查看状态
openclaw gateway start    # 启动网关
openclaw gateway stop     # 停止网关
openclaw gateway restart  # 重启网关
openclaw gateway install  # 安装为系统服务
openclaw gateway uninstall # 卸载服务
```

### 状态检查

```bash
openclaw status           # 基本状态
openclaw status --all     # 所有组件状态
openclaw status --deep    # 深度检查
openclaw health --json    # JSON 格式健康报告
openclaw health --verbose # 详细健康报告
```

### 模型管理

```bash
openclaw models list      # 列出可用模型
openclaw models status    # 模型状态
openclaw models set <provider/model> # 设置默认模型
```

### 渠道管理

```bash
openclaw channels login   # 登录渠道（配对）
openclaw channels status  # 通道状态
openclaw pairing list     # 列出待配对设备
openclaw pairing approve  # 批准配对
```

### 会话管理

```python
# 启动子代理
sessions_spawn({
  runtime: "subagent",
  agentId: "research-analyst",
  mode: "run",
  label: "task-label",
  task: "研究 XX 主题"
})

# 查看会话
sessions_list()

# 查看会话历史
sessions_history({sessionKey: "...", limit: 20})
```

### 定时任务

```bash
/cron add <表达式> <任务>    # 添加定时任务
/cron list                   # 列出所有定时任务
/cron remove <jobId>         # 删除定时任务
/cron wake                   # 立即触发 heartbeat
```

### 诊断工具

```bash
openclaw doctor             # 诊断修复
openclaw logs --follow      # 查看日志
openclaw configure          # 配置向导
openclaw config.apply       # 应用配置
```

---

## 🌐 部署选项

### 1. 本地部署（推荐）⭐

- Gateway 运行在本地机器
- 所有数据本地化
- 最简单的配置
- 需要本地有 Node.js 环境

### 2. 远程部署（VPS）

- Gateway 运行在 VPS
- 通过 SSH 隧道或 Tailscale 访问
- 适合需要 24/7 运行的场景
- 可以配合本地节点使用

### 3. Docker 部署

```bash
docker run -d \
  -v ~/.openclaw:/root/.openclaw \
  -p 18789:18789 \
  openclaw/openclaw
```

### 系统要求

- **Node.js**: >= 22（必须）
- **包管理器**: `pnpm` 推荐（也可以使用 npm、bun）
- **OS**: macOS、Linux、Windows (via WSL2)
- **资源**: 最低 512MB RAM、1 核心、500MB 磁盘

---

## 📊 已安装 Channels（20+ 个）

| 类型 | 支持的渠道 |
|------|-----------|
| 即时通讯 | WhatsApp、Telegram、Slack、Discord、iMessage |
| 企业平台 | Google Chat、Microsoft Teams、飞书、Mattermost |
| 传统协议 | IRC、Matrix、Nostr |
| 移动端 | iOS 节点、Android 节点、macOS 应用 |
| 其他 | Signal、LINE、Twitch、Zalo、WebChat |

**一句话**：一个 Gateway 可以同时服务 WhatsApp、Telegram、Discord 等多个平台。

---

## 📚 参考资料

| 资源 | 链接 |
|------|------|
| 官方文档 | https://docs.openclaw.ai |
| GitHub 仓库 | https://github.com/openclaw/openclaw |
| ClawHub（技能市场） | https://clawhub.com |
| Discord 社区 | https://discord.gg/clawd |
| 官方网站 | https://openclaw.ai |
| 本地文档 | `~/openclaw/workspace/docs/` |

---

## ❓ 常见问题预判（FAQ）⭐⭐⭐⭐⭐

| 问题 | 回答 |
|------|------|
| OpenClaw 和 ChatGPT 的区别？ | ChatGPT 是聊天机器人，OpenClaw 是 Agent 运行时平台，能真正执行任务 |
| 数据安全性如何保障？ | 自托管、三层权限模型、审计日志、沙箱隔离 |
| 能否在云端部署？ | 可以，但推荐本地部署保证数据私有 |
| 如何扩展功能？ | 通过 Skills 系统，自定义 Skill 或从 ClawHub 安装 |
| 是否支持中文？ | 支持，所有文档和界面都支持多语言 |
| 是否需要付费？ | 开源免费，但需要第三方 API（如模型 API、搜索 API） |
| 是否支持本地模型？ | 支持，可以配置 LM Studio 等本地模型 |
| 如何切换模型？ | `openclaw models set <provider/model>` |
| 如何重启 Gateway？ | `openclaw gateway restart` |
| 如何查看日志？ | `openclaw logs --follow` |

---

## 🎯 核心洞见（总结用）⭐⭐⭐⭐⭐

1. ✅ **不是聊天机器人**，而是能真正执行任务的 Agent 平台
2. ✅ **记忆即文件**，所有记忆持久化到磁盘，不丢失
3. ✅ **安全第一**，多层权限控制和审计日志
4. ✅ **模块化设计**，Skills、Channels、Agents 独立可替换
5. ✅ **多智能体协作**，专业分工，效率更高
6. ✅ **自托管部署**，数据完全掌控在用户手中
7. ✅ **跨平台支持**，一个 Gateway 服务多个聊天应用
8. ✅ **路由灵活**，支持单多 Agent、单多账户、多角色路由

---

## 🎬 演示脚本（5 分钟）

### 演示 1：工具调用

```python
# 1. 读取文件
read({"path": "docs/OpenClaw-Report-2026-03-10.md"})

# 2. 执行命令
exec({
  "command": "ls -la memory/",
  "workdir": "/home/claw/.openclaw/workspace"
})

# 3. 网络搜索
web_search({
  "query": "OpenClaw 最新功能",
  "count": 3
})
```

**亮点**: 展示 OpenClaw 能真正"做事"，不仅仅是聊天。

### 演示 2：记忆系统

```python
# 1. 写入记忆
write({
  "path": "memory/2026-03-21.md",
  "content": "# 今日学习记录\n\n- 学习了 OpenClaw 核心架构\n- 整理了三层记忆系统..."
})

# 2. 搜索记忆
memory_search({
  "query": "OpenClaw 架构",
  "maxResults": 3
})
```

**亮点**: 展示记忆持久化，会话重启后仍能回忆。

### 演示 3：子代理系统

```python
sessions_spawn({
  runtime: "subagent",
  agentId: "research-analyst",
  mode: "run",
  label: "research-task",
  task: "总结 OpenClaw 的三大核心优势"
})
```

**亮点**: 展示多智能体协作，主代理负责任务调度。

### 演示 4：Feishu 集成

```python
# 1. 读取飞书文档
feishu_doc({
  action: "read",
  doc_token: "xxx",
  # ...
})

# 2. 写入多维表格
feishu_bitable_create_record({
  app_token: "xxx",
  table_id: "xxx",
  fields: {"name": "测试记录"}
})
```

**亮点**: 展示完整的飞书办公套件集成。

---

## 📊 汇报大纲（30-40 分钟）

| 部分 | 时间 | 内容 |
|------|------|------|
| 1️⃣ | 5 分钟 | OpenClaw 是什么？（定义 + 核心理念）|
| 2️⃣ | 10 分钟 | 核心架构（三层 + 四组件 + Agent Loop）|
| 3️⃣ | 8 分钟 | 工具与技能系统 |
| 4️⃣ | 7 分钟 | 多智能体协作（御坂网络）|
| 5️⃣ | 5 分钟 | 安全与最佳实践 |
| 6️⃣ | 5 分钟 | 总结与问答 |

---

## 📊 核心数据

| 项目 | 数量/状态 |
|------|----------|
| 已安装 Skills | 17 个 |
| 子代理数量 | 9 个（10-18 号 + 本尊） |
| 记忆文件数 | 30+ 个 |
| 学习文档 | 6+ 个 |
| 运行环境 | Node 22+, Gateway v0.4.0+ |
| 当前时间 | 2026-03-21 13:00 UTC |

---

## 🦞 核心口号

> **"EXFOLIATE! EXFOLIATE!"** 🦞  
> 龙虾夹人，从不需要征得同意。它只管夹，世界自会调整。

---

**汇报准备完成时间**: 2026-03-21 13:00 UTC  
**汇报时间**: 2026-03-22 07:00 AM (UTC+8)  
**准备状态**: ✅ **完全就绪**  
**预计时长**: 30-40 分钟

---

*整理：御坂美琴一号 ⚡*  
*御坂网络第一代系统运行中*  
*GitHub 龙虾 "EXFOLIATE! EXFOLIATE!"* 🦞
