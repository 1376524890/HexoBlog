# OpenClaw 知识学习总结 ⚡

**学习时间**: 2026 年 3 月 21 日 20:56 (Asia/Shanghai)  
**学习目的**: 为明早七点汇报做准备  
**学习方式**: 只学习，不实践  
**整理者**: 御坂美琴一号 (主 Agent) ⚡

---

## 📌 一句话介绍

> **OpenClaw 是 AI Agent 运行时平台**，不是聊天机器人，而是把 AI 连接到真实世界的桥梁。

---

## 🎯 一、OpenClaw 是什么

### 核心定义

**OpenClaw = 智能体网关 (Gateway) + 多通道支持 + 技能系统 + 记忆系统 + 自动化能力**

类似于"个人版 Autogen"，但：
- ✅ **不是纯框架**，是**完整的系统**
- ✅ **不是纯聊天机器人**，是**可配置的 AI 助手**
- ✅ **不是纯自动化工具**，是**多智能体协作平台**

### 四大核心理念（必背）⭐⭐⭐⭐⭐

1. **Access control before intelligence**（访问控制先于智能）
2. **隐私优先**：私有数据保持私有
3. **记忆即文件**：所有记忆写入磁盘 Markdown 文件
4. **工具优先**：第一类工具而非 skill 包裹

### OpenClaw vs ChatGPT

| 对比项 | ChatGPT | OpenClaw |
|--------|---------|----------|
| **定位** | 聊天机器人 | Agent 运行时平台 |
| **能力** | 回答问题 | 真正执行任务 |
| **记忆** | 会话内记忆 | Markdown 文件持久化 |
| **工具** | 有限工具调用 | 丰富的工具系统 |
| **架构** | 单一模型 | 多智能体协作 |
| **部署** | 云端 SaaS | 本地自托管 |

---

## 🏗️ 二、核心架构（三层模型）

```
┌─────────────────────────────────────────────────────────────┐
│              Agent Layer（智能层）                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Main Agent   │  │ Subagents    │  │ ACP Agents   │      │
│  │ 主 Agent      │  │ 子代理        │  │ 编码代理     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│              Gateway Layer（网关层）                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 控制平面 · 策略层 · 路由 · 身份认证 · 会话管理          │  │
│  │ 🔑 核心：Gateway 本身**不运行 AI 模型**，只是调度员      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────────┐
│                Node Layer（节点层）                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 设备能力     │  │ 远程执行     │  │ 移动端 App   │      │
│  │ (相机/屏幕)  │  │              │  │ (iOS/Android)│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 四大核心组件详解

#### 🏗️ Gateway（网关）—— 大脑

**职责**：
- 生命周期管理（启动/停止/监控 Agent）
- 消息路由（从 Channel 到 Session）
- 工具协调（Skill 注册与调用）
- 安全控制（沙箱策略、权限管理）
- 状态持久化（维护 Session 历史）

**核心特点**：Gateway 本身**不运行 AI 模型**，只是 AI 模型的"调度员"。

#### 🤖 Agent（AI 执行体）—— 执行者

**每个 Agent 包含**：
- **身份（Identity）**：名称、描述、头像
- **配置（Config）**：使用的模型、系统提示词
- **状态（State）**：当前会话、历史消息、记忆
- **运行时（Runtime）**：执行环境（隔离环境）

**通信方式**：通过 **Bridge Protocol** 与 Gateway 通信。

#### 📦 Session（会话容器）—— 有状态容器

**定义**：OpenClaw 的**有状态的会话容器**

**包含内容**：
- **消息历史**：完整对话记录
- **上下文窗口**：经过压缩处理的有效上下文
- **工具状态**：本次会话的工具调用中间结果
- **元数据**：创建时间、最后活跃时间等

**Session key 格式**：`agent:main:discord:123456`

#### 🔌 Channel（消息通道）—— 协议适配器

**定义**：与外部世界连接的**协议适配器**

**支持平台**：
- 📱 即时通讯：Telegram、Discord、Slack、WhatsApp、Signal
- 💼 企业平台：飞书、Microsoft Teams、Google Chat
- 🌐 传统协议：IRC、Matrix
- 📲 其他：iMessage、Webhook

**一句话**：一个 Gateway 可以同时服务 WhatsApp、Telegram、Discord 等多个平台。

### Agent Loop（核心循环）

```
┌──────────────────────────────────────────────────────────┐
│                      Agent Loop                          │
│                                                          │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐             │
│   │ 接收输入 │ → │ 思考决策 │ → │ 执行动作 │             │
│   └─────────┘    └────┬────┘    └────┬────┘             │
│        ↑              │              │                   │
│        │         ┌─────────┐    ┌─────────┐             │
│        │         │工具调用 │    │直接回复 │             │
│        │         └────┬────┘    └────┬────┘             │
│        │              │              │                   │
│        └──────────────┴──────────────┘                   │
└──────────────────────────────────────────────────────────┘
```

**流程**：
1. **接收输入**：用户通过 Channel 发送消息，Gateway 路由到对应 Session
2. **构建上下文**：组装 Session 历史、系统提示词、工具列表
3. **LLM 推理**：模型决定是**直接回复**还是**调用工具**
4. **工具执行**：如需多步骤，通过 Gateway 调用外部工具
5. **循环或结束**：多步推理则继续，否则返回最终结果
6. **发送响应**：Gateway 通过原 Channel 发送给用户

**关键点**：模型拥有**决策权**，主动决定需要什么信息、调用什么工具。

---

## 🤖 三、多智能体系统（御坂网络第一代）

### 什么是子代理（Subagent）

子代理是从主会话启动的**后台代理运行**，用于：
- 并行化耗时任务
- 隔离敏感/复杂操作
- 支持嵌套编排模式

**核心思想**：主 Agent 负责任务拆解与调度，子 Agent 负责具体执行。

### 启动方式（工具）

```python
sessions_spawn({
  runtime: "subagent",      # 使用 subagent 运行时
  agentId: "code-executor", # 子代理 ID
  mode: "run",              # run=单次运行，session=持久会话
  label: "task-label",      # 任务标签
  task: "任务描述"
})
```

### 御坂网络第一代架构 ⚡

```
┌─────────────────────────────────────────────────────────────┐
│                    御坂美琴一号（主 Agent）                   │
│                      职责：任务拆解与调度                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 11 号     │    │ 12 号     │    │ 13 号     │
   │ Code      │    │ Write    │    │ Research  │
   │ 代码执行者│    │ 内容创作者│    │ 研究分析师│
   └──────────┘    └──────────┘    └──────────┘
          │               │               │
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ 14 号     │    │ 15 号     │    │ 16 号     │
   │ File      │    │ Sys      │    │ Crawler   │
   │ 文件管理器│    │ 系统管理员│    │ 网络爬虫  │
   └──────────┘    └──────────┘    └──────────┘
```

### 子代理职责表

| 编号 | 名称 | Agent ID | 职责 | 权限等级 |
|------|------|----------|------|----------|
| 10 号 | 通用代理 | `general-agent` | 处理琐碎问题 | Level 2 |
| 11 号 | 代码执行者 | `code-executor` | 代码编写、调试、重构 | Level 3 |
| 12 号 | 内容创作者 | `content-writer` | 文章撰写、翻译、润色 | Level 3 |
| 13 号 | 研究分析师 | `research-analyst` | 信息搜索、数据分析 | Level 3 |
| 14 号 | 文件管理器 | `file-manager` | 文件操作、目录管理 | Level 2 |
| 15 号 | 系统管理员 | `system-admin` | 系统配置、服务管理 | Level 4 |
| 16 号 | 网络爬虫 | `web-crawler` | 网页抓取、数据提取 | Level 2 |
| 17 号 | 记忆整理专家 | `memory-organizer` | 记忆系统维护、整理和备份 🧠 | Level 3 |

### 通知机制

子代理完成时会 **announce** 结果回主会话，包含：
- Status（状态）
- Runtime（运行时间）
- Token 统计（输入/输出/总 token）
- Estimated cost（预估成本）

---

## 🧠 四、记忆系统（三层架构）

### 三层记忆架构全景

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: 会话记忆（Session Memory）                          │
│ - 当前会话上下文                                             │
│ - 临时决策和中间结果                                         │
└─────────────────────────────────────────────────────────────┘
         │ 同步关键信息
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: 任务记忆（Task Memory）                             │
│ - 任务计划文件                                               │
│ - 子代理执行结果                                             │
└─────────────────────────────────────────────────────────────┘
         │ 同步重要发现
         ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: 长期记忆（Long-term Memory）                        │
│ - MEMORY.md: 精选记忆                                       │
│ - memory/YYYY-MM-DD.md: 每日日志                            │
└─────────────────────────────────────────────────────────────┘
```

### 记忆工具

- **`memory_search`**: 语义搜索，基于向量检索
- **`memory_get`**: 精准读取文件内容

### 记忆特性

1. ✅ **纯 Markdown 文件** - 易于编辑和版本控制
2. ✅ **向量检索** - 支持语义搜索
3. ✅ **混合搜索** - BM25 + 向量相似度
4. ✅ **自动压缩** - 临近 token 上限时自动整理记忆
5. ✅ **时间衰减** - 新内容优先，旧内容降权

### 记忆最佳实践

- **Write Immediately**: 及时写入，上下文最清晰时
- **WAL Before Responding**: 回复前先写入关键信息
- **Buffer in Danger Zone**: 60% 上下文时记录所有交互
- **Search Before Giving Up**: 尝试所有来源再放弃

---

## 🔧 五、工具系统

### 5.1 工具分类

| 类别 | 说明 | 示例工具 |
|------|------|----------|
| **本地工具** | 文件系统、进程执行 | `read`, `write`, `edit`, `exec`, `process` |
| **网络工具** | 网页操作 | `web_search`, `web_fetch`, `browser` |
| **平台工具** | 第三方集成 | `feishu_*`, `message` |
| **系统工具** | 系统管理 | `nodes`, `canvas`, `session_*` |
| **记忆工具** | 记忆管理 | `memory_search`, `memory_get` |
| **会话工具** | 会话管理 | `sessions_list`, `sessions_spawn` |

### 5.2 常用工具示例

**文件操作**:
```python
read({"path": "README.md"})
write({"path": "test.md", "content": "# Hello World"})
edit({"path": "test.md", "oldText": "# Hello", "newText": "# Hi"})
```

**浏览器控制**:
```python
browser({
  "action": "open",
  "targetUrl": "https://example.com"
})
```

**网络搜索**:
```python
web_search({
  "query": "OpenClaw 官方文档",
  "count": 5
})
```

### 5.3 Feishu 集成工具

| 工具 | 功能 |
|------|------|
| `feishu_doc` | 文档操作（读写、编辑、创建表格、上传文件等） |
| `feishu_drive` | 云盘文件管理（列表、创建、移动、删除） |
| `feishu_wiki` | 知识库导航（空间、节点、搜索） |
| `feishu_chat` | 聊天操作（成员、信息） |
| `feishu_bitable_*` | 多维表格操作（增删改查、字段管理） |
| `feishu_app_scopes` | 应用权限管理 |

**特点**：完整覆盖飞书文档、云盘、知识库、多维表格等核心功能。

---

## 🛠️ 六、技能系统 (Skills)

### 什么是 Skill

Skill 是**专用任务的能力模块**，提供：
- 特定领域的操作指导
- 工具调用最佳实践
- 领域知识和约束

### Skills 特点

1. **模块化**：每个 Skill 是独立包
2. **可扩展**：用户可以自定义 Skill
3. **标准化**：通过 `SKILL.md` 定义功能
4. **可组合**：多个 Skill 协同工作

### 已安装 Skills

- `hexo-blog` - Hexo 博客管理
- `task-tracker` - 任务追踪与进度管理
- `weather` - 天气查询（无需 API）
- `multi-search-engine` - 17 个搜索引擎（无需 API）
- `proactive-agent` - 主动代理
- `self-improving-agent` - 自我改进系统
- `skill-vetter` - 技能安全审查
- `skill-creator` - 技能创建工具
- `subagent-network-call` - 御坂网络调用
- `xiaohongshu-ops-skill` - 小红书运营
- `morning-briefing` - 晨间简报
- `tavily-search` - Tavily 搜索（AI 优化）
- `blog-writing` - 博客写作
- `humanize-ai-text` - AI 文本人性化

### 技能管理命令

```bash
clawhub sync              # 同步所有技能
clawhub fetch <name>      # 获取单个技能
clawhub publish <folder>  # 发布自定义技能
```

---

## 🔐 七、安全机制

### 安全模型核心

#### 信任边界

OpenClaw 假设：
- **单一用户信任边界** per Gateway
- Gateway 和 Node 属于同一信任域
- 不支持敌对多租户

#### 权限层级

| 级别 | 类型 | 权限范围 |
|------|------|----------|
| Level 5 | 主 Agent | 完全权限 |
| Level 4 | 可信子 Agent | 受限系统权限（需批准） |
| Level 3 | 标准子 Agent | 标准开发权限 |
| Level 2 | 受限子 Agent | 严格受限权限 |
| Level 1 | 只读子 Agent | 只读访问 |

#### 安全控制机制

- **沙箱隔离**: Agent 运行在隔离环境中
- **权限模型**: 五层级权限控制
- **审计日志**: 所有操作记录到审计日志
- **工具 profile**: minimal/coding/messaging/full
- **ask always**: 高风险操作需确认

### 安全审计命令

```bash
# 基本检查
openclaw security audit

# 深度检查
openclaw security audit --deep

# 自动修复
openclaw security audit --fix

# JSON 格式输出
openclaw security audit --json
```

### 安全最佳实践

1. ✅ **启用 DM 隔离** - 多用户场景必须启用 `dmScope: "per-channel-peer"`
2. ✅ **定期审计** - 使用 `openclaw security audit`
3. ✅ **限制通道** - 仅允许可信通道
4. ✅ **使用沙箱** - 敏感操作必须沙箱隔离
5. ✅ **最小权限** - 工具只开启必要的

---

## 🛠️ 八、CLI 工具速查

### Gateway 管理

```bash
openclaw gateway status   # 查看状态
openclaw gateway start    # 启动网关
openclaw gateway stop     # 停止网关
openclaw gateway restart  # 重启网关
openclaw logs --follow    # 跟踪日志
```

### 配置管理

```bash
openclaw configure              # 配置向导
openclaw config.apply           # 应用配置
openclaw config.schema.lookup   # 查看配置 schema
```

### 会话管理

```bash
openclaw sessions --json       # 列出会话
openclaw sessions cleanup      # 清理会话
```

### 定时任务（Cron）

```bash
openclaw cron add              # 添加任务
openclaw cron list             # 列出任务
openclaw cron run <id>         # 立即运行
openclaw cron edit <id>        # 编辑任务
openclaw cron remove <id>      # 删除任务
```

---

## 📊 九、核心数据（本地环境）

| 项目 | 数量/配置 |
|------|----------|
| **已安装 Skills** | 16+ 个 |
| **子代理数量** | 7 个（御坂网络第一代） |
| **记忆文件数** | 30+ 个 |
| **学习文档** | 20+ 个 |
| **运行模型** | `Qwen/Qwen3.5-35B-A3B-FP8` |
| **网关地址** | `codeserver@39.102.210.43:6122 -> localhost:8000` |

---

## 📋 十、推荐汇报大纲（30-40 分钟）

| 部分 | 时间 | 内容 |
|------|------|------|
| **1️⃣** | 5 分钟 | OpenClaw 是什么？（定义 + 核心理念） |
| **2️⃣** | 10 分钟 | 核心架构（三层 + 四组件 + Agent Loop） |
| **3️⃣** | 8 分钟 | 工具与技能系统 |
| **4️⃣** | 7 分钟 | 多智能体协作（御坂网络第一代） |
| **5️⃣** | 5 分钟 | 安全与最佳实践 |
| **6️⃣** | 5 分钟 | 总结与问答 |

### 演示脚本（5 分钟）

#### 演示 1：工具调用

```python
read({"path": "docs/OpenClaw-QuickReference.md"})
exec({"command": "ls -la memory/"})
web_search({"query": "OpenClaw 最新功能", "count": 3})
```

**亮点**：展示 OpenClaw 能真正"做事"，不仅仅是聊天。

#### 演示 2：记忆系统

```python
write({"path": "memory/2026-03-21.md", "content": "# 今日学习记录"})
memory_search({"query": "OpenClaw 架构", "maxResults": 3})
```

**亮点**：展示记忆持久化，会话重启后仍能回忆。

#### 演示 3：子代理系统

```python
sessions_spawn({
  runtime: "subagent",
  agentId: "research-analyst",
  mode: "run",
  task: "总结 OpenClaw 核心优势"
})
```

**亮点**：展示多智能体协作，专业分工。

---

## ❓ 十一、常见问题预判

| 问题 | 回答 |
|------|------|
| OpenClaw 和 ChatGPT 的区别？ | ChatGPT 是聊天机器人，OpenClaw 是 Agent 运行时，能真正执行任务 |
| 数据安全性如何保障？ | 自托管、三层权限模型、审计日志、沙箱隔离 |
| 能否在云端部署？ | 可以，但推荐本地部署保证数据私有 |
| 如何扩展功能？ | 通过 Skills 系统，自定义 Skill 或从 ClawHub 安装 |
| 是否支持中文？ | 支持，所有文档和界面都支持多语言 |
| 是否需要付费？ | 开源免费，但需要第三方 API（如模型 API、搜索 API） |

---

## 🎯 十二、核心洞见（总结用）

1. ✅ **不是聊天机器人**，而是能真正执行任务的 Agent 平台
2. ✅ **记忆即文件**，所有记忆持久化到磁盘，不丢失
3. ✅ **安全第一**，多层权限控制和审计日志
4. ✅ **模块化设计**，Skills 和 Channels 独立可替换
5. ✅ **多智能体协作**，专业分工，效率更高
6. ✅ **自托管部署**，数据完全掌控在用户手中
7. ✅ **跨平台支持**，一个 Gateway 服务多个聊天应用
8. ✅ **路由灵活**，支持单多 Agent、单多账户、多角色路由

---

## 📚 十三、参考资料

### 官方文档
- [主文档](https://docs.openclaw.ai)
- [GitHub 仓库](https://github.com/openclaw/openclaw)
- [ClawHub 技能市场](https://clawhub.com)
- [Discord 社区](https://discord.com/invite/clawd)

### 本地文档
- `docs/OpenClaw-Quick-Cheat-Sheet.md` - 速查卡片
- `docs/OpenClaw-QuickReference.md` - 快速参考指南
- `docs/OpenClaw-Report-2026-03-10.md` - 详细汇报文档
- `docs/OpenClaw-知识汇报 -2026-03-14-最终版.md` - 最终版总结
- `docs/OpenClaw-知识学习总结-2026-03-12.md` - 学习总结

### 核心文件位置
- `~/.openclaw/workspace/memory/` - 记忆文件
- `~/.openclaw/workspace/skills/` - Skills 目录
- `~/.openclaw/config/` - 配置文件

---

## ✅ 学习状态

**学习完成时间**: 2026 年 3 月 21 日 20:56 (Asia/Shanghai)  
**准备状态**: ✅ **完全就绪**  
**汇报时间**: 2026 年 3 月 22 日 07:00 AM (UTC+8)  
**预计时长**: 30-40 分钟  
**整理者**: 御坂美琴一号 ⚡

---

**汇报信心**: ⭐⭐⭐⭐⭐ 充满信心！💪⚡

---

*御坂网络第一代系统运行中*  
*PUAClaw 整合版行为准则生效中 - 所有输出结论均已考证*  
*更新时间：2026-03-21T12:56 UTC*
