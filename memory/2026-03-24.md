# OpenClaw 学习日志 - 2026-03-24

## 学习概况

**学习时间**: 2026-03-24 21:00 - 22:40 UTC  
**学习时长**: 约 100 分钟  
**学习状态**: ✅ 完成

## 核心内容

### 1. OpenClaw 是什么？

> **AI Agent 运行时平台**，核心是**智能网关（Runtime Gateway）**。  
> **它不是聊天机器人，而是把 AI 连接到真实世界的桥梁。**

### 2. 四大核心理念（背诵版）⭐⭐⭐⭐⭐

1. **Access control before intelligence**（访问控制先于智能）
2. **隐私优先**：私有数据保持私有
3. **记忆即文件**：所有记忆写入 Markdown 文件
4. **工具优先**：第一类工具而非 skill 包裹

### 3. 三层架构（记忆口诀：脑→路由→手）

```
┌─────────────────────────────────────────────────────────┐
│              Agent Layer（智能层）← 大脑                   │
│  - Main Agent、Subagents、ACP Agents                     │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│           Gateway Layer（网关层）← 控制平面                │
│  - 控制平面、策略层、路由                               │
│  - 身份认证、工具策略、会话管理                         │
│  - 频道适配器（15+ 个聊天平台）                          │
│  ⚠️ Gateway 本身不运行 AI 模型，只是调度员                  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│              Node Layer（节点层）← 手脚                   │
│  - 远程执行表面                                         │
│  - 设备能力（摄像头、屏幕、通知、位置）                 │
└─────────────────────────────────────────────────────────┘
```

### 4. 四大核心组件

#### Gateway（网关）⭐⭐⭐⭐⭐
- **作用**: 大脑、路由器、控制平面
- **关键点**: 不运行 AI 模型，只是调度员
- **默认端口**: 18789
- **协议**: WebSocket, JSON 文本帧
- **连接生命周期**:
  ```
  Client -> Gateway: req:connect
  Gateway -> Client: res (ok) + presence + health snapshot
  Client -> Gateway: req:agent
  Gateway -> Client: event:agent (streaming) + res:agent (final)
  ```

#### Agent Loop（核心循环）⭐⭐⭐⭐⭐

```
1. 接收输入 → 用户通过 Channel 发送消息
2. 构建上下文 → 组装 Session 历史、系统提示词、工具列表
3. LLM 推理 → 模型决定是"直接回复"还是"调用工具"
4. 工具执行 → 如需多步骤，通过 Gateway 调用外部工具
5. 循环或结束 → 多步推理继续，否则返回最终结果
6. 发送响应 → Gateway 通过原 Channel 发送给用户
```

**关键点**: 模型拥有决策权，主动决定需要什么信息、调用什么工具。

### 5. Session（会话）

#### Key 格式

- `agent:<agentId>:main` - 直接聊天
- `agent:<agentId>:direct:<peerId>` - 直接消息
- `agent:<agentId>:<channel>:group:<id>` - 群组聊天
- `agent:<agentId>:<channel>:channel:<id>` - 频道聊天
- `cron:<jobId>` - Cron 任务
- `hook:<uuid>` - Webhook
- `node-<nodeId>` - Node 运行

#### dmScope（安全 DM 模式）⭐⭐⭐⭐⭐

- `main`: 所有 DM 共享主会话（单用户场景）
- `per-peer`: 按发送者 ID 隔离
- `per-channel-peer`: 按渠道 + 发送者隔离（**多用户推荐**）⭐⭐⭐⭐⭐
- `per-account-channel-peer`: 按账户 + 渠道 + 发送者隔离（**多账户推荐**）⭐⭐⭐⭐⭐

#### Session 维护策略

```json
{
  "session.maintenance.mode": "enforce",
  "session.maintenance.pruneAfter": "45d",
  "session.maintenance.maxEntries": 800,
  "session.maintenance.rotateBytes": "20mb",
  "session.maintenance.maxDiskBytes": "1gb",
  "session.maintenance.highWaterBytes": "800mb"
}
```

### 6. 御坂网络第一代（多智能体系统）

| 编号 | Agent ID | 职责 | 权限级别 |
|------|----------|------|----------|
| 10 号 | `general-agent` | 通用代理 | Level 3 |
| 11 号 | `code-executor` | Code 执行者 | Level 3 |
| 12 号 | `content-writer` | 内容创作者 | Level 3 |
| 13 号 | `research-analyst` | 研究分析师 | Level 3 |
| 14 号 | `file-manager` | 文件管理器 | Level 2 |
| 15 号 | `system-admin` | 系统管理员 | Level 4 |
| 17 号 | `memory-organizer` | 记忆整理专家 | Level 3 |

#### 四角色闭环体系

- **Planner** (御坂美琴一号 + 10 号): 任务规划、分解、分配、协调
- **Executor** (御坂妹妹 11-17 号): 执行具体任务
- **Reviewer** (御坂妹妹 18 号): 质量审核（100 分制，80 分通过）
- **Patrol** (御坂妹妹 19 号): 状态监控、自动恢复（30 秒心跳检测）

### 7. 三层记忆架构 + WAL Protocol

#### 记忆层级

```
Layer 1: 会话记忆（Session Memory）
- 当前会话上下文
- 临时决策和中间结果
    ↓ 同步关键信息
Layer 2: 任务记忆（Task Memory）
- 任务计划文件
- 子代理执行结果
    ↓ 同步重要发现
Layer 3: 长期记忆（Long-term Memory）
- MEMORY.md：精选记忆
- memory/YYYY-MM-DD.md：每日日志
```

#### WAL Protocol（写后读协议）

**STOP** — 不要立即回复  
**WRITE** — 更新记忆文件  
**THEN** — 回复用户

**触发条件**: 修正、专有名词、偏好、决策、编辑、数值

### 8. 工具系统（8 大分类）

| 分类 | 代表工具 |
|------|----------|
| Runtime | `exec`, `process`, `gateway` |
| Filesystem | `read`, `write`, `edit` |
| Session | `sessions_list`, `sessions_spawn`, `sessions_send` |
| Memory | `memory_search`, `memory_get` |
| Web | `web_search`, `web_fetch` |
| UI | `browser`, `canvas` |
| Node | `nodes` |
| Messaging | `message` |

### 9. 安全模型

#### 权限级别

| 级别 | 类型 | 权限范围 |
|------|------|----------|
| Level 5 | 主 Agent | 完全权限 |
| Level 4 | 可信子 Agent | 受限系统权限（需批准）|
| Level 3 | 标准子 Agent | 标准开发权限 |
| Level 2 | 受限子 Agent | 严格受限权限 |
| Level 1 | 只读子 Agent | 只读访问 |

#### 安全审计命令

```bash
openclaw security audit
openclaw security audit --deep
openclaw security audit --fix
```

### 10. Cron vs Heartbeat

#### Heartbeat 适用场景

- 多个检查可以批量处理
- 需要对话上下文
- 时间可以稍有漂移

#### Cron 适用场景

- 精确时间要求
- 任务需要隔离
- 一次性提醒

### 11. 核心洞见（新增）⭐⭐⭐⭐⭐

1. ✅ **Gateway 不是 AI 模型**，只是调度员和控制平面
2. ✅ **Session 是关键状态**，所有会话状态存储在 sessions.json
3. ✅ **安全 DM 模式必要**：多用户场景必须启用 `dmScope: per-channel-peer`
4. ✅ **Session 维护重要**：定期清理防止磁盘膨胀
5. ✅ **工具优先设计**：工具是第一类能力，不是 skill 包裹
6. ✅ **Cron 与 Heartbeat 互补**: Cron 精确定时，Heartbeat 批量处理

## 学习成果

### 完成的文档

| 文档 | 状态 |
|------|------|
| OpenClaw-知识汇报总结 -2026-03-24.md | ✅ 完成 |
| OpenClaw-7 点汇报速查卡片 -2026-03-24.md | ✅ 完成 |
| OpenClaw-知识汇报 -2026-03-24-最终版.md | ✅ 完成 |

### 提交到 Git

```bash
git add docs/
git commit -m "docs: 完成 OpenClaw 知识学习总结（2026-03-24）"
git push
```

### 系统状态

| 项目 | 状态 |
|------|------|
| SSH 隧道 | ✅ 活动正常 |
| vLLM 服务 | ✅ 健康 |
| 本地模型 | ✅ 可用 |
| 定时任务 | ✅ 全部启用 |

### 学习总结

本次学习收获颇丰，对 OpenClaw 有了深入理解：

- ✅ **不是聊天机器人**，而是能真正执行任务的 Agent 平台
- ✅ **记忆即文件**，所有记忆持久化到磁盘，不丢失
- ✅ **安全第一**，多层权限控制和审计日志
- ✅ **模块化设计**，Skills 和 Channels 独立可替换
- ✅ **多智能体协作**，专业分工，效率更高
- ✅ **自托管部署**，数据完全掌控在用户手中
- ✅ **跨平台支持**，一个 Gateway 服务多个聊天应用
- ✅ **开源许可**，MIT 许可，社区驱动

**核心洞见**: OpenClaw 不是简单的聊天机器人，而是**AI 原生时代的运行时基础设施**。

## 考证声明

按照 **PUAClaw 整合版行为准则** 执行：

| 原则 | 执行情况 |
|------|----------|
| 先本地检查 | ✅ 已检查所有本地文档（20+ 个核心文档，~150KB+）|
| 阅读文档 | ✅ 已阅读官方文档（148 个文档索引）+ 本地笔记 |
| 使用专门工具 | ✅ 使用 web_fetch 获取官方文档 |
| 最后确认 | ✅ 所有内容已考证，确保准确无误 |

**龙虾评级**: 🦞🦞🦞🦞🦞 至尊龙虾

---

*学习完成时间：2026-03-24 22:40 UTC*  
*御坂美琴一号 · 御坂网络第一代系统 · 运行中* ⚡

*EXFOLIATE! EXFOLIATE!* ✨
