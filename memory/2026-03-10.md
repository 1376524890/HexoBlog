# 2026-03-10 - 御坂网络配置修复记录

## 05:03 - 配置检查启动

**请求者**: 御坂美琴本尊  
**执行人**: 御坂美琴一号  
**目标**: 检查所有 subagent 的人格、工具、记忆配置是否正确，执行工作是否使用了正确的 skill

---

## 🔍 检查发现的主要问题

### 问题 1: Skill 路径错误 🚨

**发现**：
- ❌ `web-markdown-search` skill **不存在**
- ❌ `coding-agent` skill **不存在**
- ❌ `code-executor` skill **不存在**
- ✅ `smart-search` skill **存在**于 `/home/claw/.openclaw/workspace/skills/smart-search/`

**正确路径**：
```
/home/claw/.openclaw/workspace/skills/
├── smart-search/          ✅ 已确认
├── code-executor/         ❌ 只有文件夹，没有 SKILL.md
├── complex-research-skill/
├── memory-organizer/
├── public/
└── security-audit/
```

---

### 问题 2: 任务分派流程错误 🚨

**MEMORY.md 中的错误规则**：
```markdown
**【代码编写】** → 分派给御坂妹妹 11 号 (`code-executor`)
- 御坂妹妹 11 号用 `edit` 工具直接改
- 复杂任务 → 御坂妹妹 11 号调用 Claude Code
```

**问题**：
1. ❌ `code-executor` **没有对应的 Skill 配置文件**
2. ❌ **御坂美琴一号不应该直接调用 Claude Code**！应该通过 `sessions_spawn` 分派任务！

**正确的分派方式**：
```javascript
// ✅ 正确：通过 sessions_spawn 分派
sessions_spawn({
  runtime: "subagent",
  agentId: "main",
  task: "使用 Claude Code 执行：创建一个 Python 爬虫项目"
})
```

---

### 问题 3: sessions_spawn runtime 参数错误 🚨

**MEMORY.md 中的错误**：
```javascript
// ❌ 曾经犯过的错误
{
  runtime: "acp",  // 会报错 "ACP runtime backend is not configured"
  agentId: "code-executor"
}
```

**正确的用法**：
```javascript
// ✅ 正确：使用 subagent runtime
{
  runtime: "subagent",  // 必须使用 subagent
  agentId: "web-crawler",
  task: "使用 smart-search skill 搜索 XXX",
  mode: "session"
}
```

---

### 问题 4: 御坂美琴一号职责边界错误 🚨

**MEMORY.md 中的错误规则**：
```markdown
**❌ 本小姐（御坂美琴一号）只负责：**
- 识别任务类型
- 选择合适的执行者
- 监督执行进度
- 向御坂大人汇报结果

**✅ 御坂妹妹们负责：** 具体执行、数据处理
```

**问题**：
- ❌ "御坂妹妹们负责具体执行" → 这个描述容易误解为御坂美琴一号直接让 Agent 执行

**正确理解**：
- ✅ **御坂美琴一号职责**：识别 → 分派（sessions_spawn）→ 监督 → 汇报
- ✅ **Agent 职责**：接收任务 → 调用工具/Skill → 完成任务

---

## ✅ 已完成的修复

### 1. 备份 MEMORY.md ✅

```bash
mkdir -p /home/claw/.openclaw/workspace/memory/backups
cp /home/claw/.openclaw/workspace/MEMORY.md /home/claw/.openclaw/workspace/memory/backups/MEMORY.md.backup.20260310_0503
```

---

### 2. 修正任务分派原则 ✅

**新增内容**：
- 📋 **完整任务分派规则**（网络搜索、代码编写、内容创作、数据分析、文件操作、通用任务）
- 📋 **Claude Code 使用规范**（适用场景、调用方式、常见错误、最佳实践）
- 📋 **sessions_spawn 正确用法**（必须使用 `runtime: "subagent"`）
- 📋 **御坂美琴一号职责边界**（明确什么可以做、什么不可以做）

**修正要点**：
1. ✅ 明确所有任务通过 `sessions_spawn` 分派
2. ✅ 代码编写使用 `agentId: "main"`，不是 `code-executor`
3. ✅ 搜索任务使用 `agentId: "web-crawler"`
4. ✅ 必须使用 `runtime: "subagent"`，不要用 `runtime: "acp"`
5. ✅ 复杂编程任务必须在 task 中强调"使用 Claude Code 执行"

---

### 3. 创建补充文档 ✅

**文档位置**: `/home/claw/.openclaw/workspace/docs/Claude-Code-Usage-Guide.md`

**内容**：
- 📋 Claude Code 适用场景（建项目、PR 审查、重构等）
- 📋 调用规范（正确示例、常见错误）
- 📋 命令参考（基本命令、权限模式、输出模式）
- 📋 检查清单（使用前的确认项）
- 📋 最佳实践（明确边界、提供上下文、设置超时、使用 label）

---

### 4. 更新 URL 转 Markdown 搜索技术 ✅

**修正内容**：
- ✅ 移除 `web-markdown-search` skill 的错误引用
- ✅ 明确 `smart-search` skill 的正确位置
- ✅ 强调御坂美琴一号不直接调用工具，而是通过 `sessions_spawn` 分派

---

### 5. 修正御坂网络成员列表 ✅

**修正**：
- ✅ "御坂妹妹 11-17 号" → "御坂妹妹 10-17 号"
- ✅ 更新职责描述，明确 Agent 的职责边界

---

## 📝 新的任务分派规则（完整版）

### 1. 【网络搜索】→ `sessions_spawn(agentId: "web-crawler")`

| 场景 | 分派方式 | 任务描述 |
|------|---------|---------|
| **单 URL 抓取** | `sessions_spawn(agentId: "web-crawler")` | task: "使用 web_fetch 工具抓取：https://example.com" |
| **复杂搜索** | `sessions_spawn(agentId: "web-crawler")` | task: "使用 smart-search skill 搜索：{query}" |

### 2. 【代码编写】→ `sessions_spawn(agentId: "main")`

| 任务类型 | 分派方式 | 任务描述 |
|---------|---------|---------|
| **简单修改** | `sessions_spawn(agentId: "main")` | task: "使用 edit 工具修改文件：{path}" |
| **复杂任务** | `sessions_spawn(agentId: "main")` | task: "使用 Claude Code 执行复杂编程任务" |

### 3. 【内容创作】→ `sessions_spawn(agentId: "content-writer")`

### 4. 【数据分析】→ `sessions_spawn(agentId: "research-analyst")`

### 5. 【文件操作】→ `sessions_spawn(agentId: "file-manager")`

### 6. 【通用任务】→ `sessions_spawn(agentId: "general-agent")`

---

## ⚠️ sessions_spawn 正确用法（2026-03-10 修正）

### ✅ 必须使用：`runtime: "subagent"`

```javascript
sessions_spawn({
  runtime: "subagent",      // ✅ 必须使用 subagent
  agentId: "web-crawler",   // 选择正确的 Agent
  task: "使用 smart-search skill 搜索机器学习",
  mode: "session",          // 持久化会话
  label: "搜索任务"         // 便于识别
})
```

### ❌ 不要使用：`runtime: "acp"`

```javascript
// ❌ 会报错 "ACP runtime backend is not configured"
sessions_spawn({
  runtime: "acp",
  agentId: "web-crawler",
  task: "搜索"
})
```

---

## 📋 验证清单

- [x] ✅ 备份了原始 MEMORY.md
- [x] ✅ 修正了任务分派原则
- [x] ✅ 修正了 URL 转 Markdown 搜索技术部分
- [x] ✅ 修正了御坂网络成员列表
- [x] ✅ 修正了职责描述
- [x] ✅ 创建了 Claude Code 使用规范文档
- [ ] ⏳ Git commit 和 push（待执行）

---

## 🎯 下一步行动

1. **Git 提交修复**：
   ```bash
   git add .
   git commit -m "修复 MEMORY.md 中的任务分派规则，新增 Claude Code 使用规范"
   git push
   ```

2. **测试新规则**：
   - 测试搜索任务分派给 web-crawler
   - 测试代码任务分派给 main
   - 验证 sessions_spawn 使用 subagent runtime

3. **持续优化**：
   - 记录实际使用中的问题
   - 根据需要调整分派规则
   - 定期 review 记忆文件

---

## 📊 修复统计

| 项目 | 状态 |
|------|------|
| MEMORY.md 行数 | 537 行（增加） |
| 修复内容数 | 5 个主要问题 |
| 新增文档 | 1 个（Claude-Code-Usage-Guide.md） |
| 备份文件 | 1 个（MEMOREY.md.backup.20260310_0503） |

---

*修复完成时间：2026-03-10 05:03 UTC*  
*执行人：御坂美琴一号*  
*审核人：御坂美琴本尊*
